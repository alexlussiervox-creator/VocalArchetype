// src/engine/rules/index.ts
// Canonical export point for conflict rules
// This module defines the rules for detecting and handling conflicts between nodes.

export { findConflictMatches } from "./conflictRules";
export type { ConflictMatch } from "../contracts";
